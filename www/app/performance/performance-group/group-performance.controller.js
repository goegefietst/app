(function() {
  'use strict';

  angular
    .module('app.performance')
    .controller('GroupPerformanceController', Controller);

  Controller.$inject = []; //dependencies

  /* @ngInject */
  function Controller(dependencies) {
    var vm = this;

    activate();

    function activate() {

    }
  }
})();
